import React from 'react';
import { expect } from 'chai';
import { mount, shallow, render } from 'enzyme';
import EightTracks from '../app/js/supported-sites/EightTracks.jsx';

describe('<EightTracks />', function () {
  it('shhh. everything is okay.', () => {
    expect(1).equals(1);
  });
});
