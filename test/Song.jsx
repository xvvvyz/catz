import React from 'react';
import { expect } from 'chai';
import { mount, shallow, render } from 'enzyme';
import EightTracks from '../app/js/media-types/Song.jsx';

describe('<Song />', function () {
  it('shhh. everything is okay.', () => {
    expect(1).equals(1);
  });
});
